import { ForClickDirective } from './for-click.directive';

describe('ForClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ForClickDirective();
    expect(directive).toBeTruthy();
  });
});
